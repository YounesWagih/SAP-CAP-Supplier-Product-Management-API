{
  "name": "cap-task",
  "version": "1.0.0",
  "description": "SAP CAP-based Supplier Product Management API",
  "scripts": {
    "start": "cds serve",
    "watch": "cds watch",
    "build": "cds build",
    "deploy": "cds deploy",
    "test": "jest"
  },
  "dependencies": {
    "@sap/cds": "^8.0.0",
    "@sap/cds-hana": "^2.0.0",
    "@sap/cds-sqlite": "^1.0.0",
    "express": "^4.18.2",
    "sqlite3": "^6.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite"
      }
    }
  }
}
